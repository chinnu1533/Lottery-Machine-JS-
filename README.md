# Lottery-Machine-JS-
how to run the file:
install node JS
in vs code type "npm init", then install the package "npm i prompt-sync"
now in teminal locate your file and run it by typing node then file name
